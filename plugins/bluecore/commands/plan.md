---
name: plan
description: 要件言い換え→リスク評価→段階的実装計画作成。コード前にユーザー確認待ち。
command: /plan
---

<!-- DRY: grillme 前段（発火〜他処理に進まない）は全コマンド共通。終了条件・永続メモリ・引数は固有 -->

# 実装計画

## grillme 強制起動（必須）

開始直後に grillme スキルで共通理解を固め、完了まで他処理に進まない。完了時は合意方針を1行サマリで確認する。

## 永続メモリ

- context: SessionStart で `<mem-context>` 自動注入
- search: `{要件のキーワード}` / `risk blocker error {関連キーワード}`
- record: `{"event_type": "plan", "content": "{計画サマリー}"}`

## skill 起動メカニズム

`loop-dev` は `user-invocable: false` の skill。本文で「loop-dev skill を起動」と明示することで Skill ツール経由の fork 実行で発火する。

## 使用タイミング

- 新機能開始・大きなアーキテクチャ変更・複雑なリファクタリング
- **変更対象ファイルが5件以上のリファクタリング**
- 複数ファイル/コンポーネントに影響する変更・要件が曖昧な時

## ステップ1: 要件言い換え

ユーザー入力を「何を作るか」「なぜ作るか」「成功条件は何か」の3点で言い換え、ユーザーに認識合わせ。

## ステップ2: 過去類似実装検索

`<mem-context>` と mem search で類似作業を参照。再利用可能な既存実装・パターン・知見を抽出。

## ステップ3: リスク特定（並列）

以下の2エージェントを**同時起動**し、両結果をマージ:

- **`bluecore:planner`**: 要件言い換え・手順分解・依存関係特定・複雑度見積もり
- **`bluecore:architect`（評価モード）**: アーキテクチャ影響調査・技術的リスク洗い出し・代替設計案提示

architect の動作モード:
- 評価モード（既定）= リスクと代替案を列挙
- 決定モード = 単一推奨を確定（`/feat-dev` 等で使用）

## ステップ4: 段階的計画作成

統合結果を以下の形式で提示:

1. 要件言い換え
2. 手順分解（フェーズ別）
3. 依存関係特定
4. リスク評価
5. 複雑度見積もり（High/Medium/Low）
6. 変更ファイル一覧（正確なパス）
7. 変更しないファイル一覧（隣接するが対象外）
8. テストコマンド

## ステップ5: 確認待ち

ユーザーの肯定応答（「はい」「進めて」等）まで**コードを書かない**。

計画の修正依頼は以下の形式で受け付ける:
- "修正: [変更内容]"
- "別アプローチ: [代替案]"
- "フェーズ N を飛ばしてフェーズ M を先に進める"

肯定応答受領 → loop-dev へ移行。

## 連携

- 実装: ユーザー承認後、`loop-dev` skill を起動（入力: `task` = 承認済み計画の実装要件サマリー（1〜3 文） / `task_type` = 計画の性質に応じて `feature` | `bugfix` | `test` | `refactor-fix` から選択 / `approved_plan` = 承認済み計画で plan 段縮退。変更ファイル一覧・テストコマンドを引き継ぐ）
- loop-dev から収束 or 停止報告を受領後、Loop-Dev Result を転記して要約を提示
- 完成後: `/review`

## 引数

- 位置 #1: `[要件説明]`（省略時: 直前の会話文脈から要件抽出）
